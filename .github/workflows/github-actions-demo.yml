- name: WakaTime Stats
  uses: athul/waka-readme@master
  with:
    WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
    SHOW_TITLE: false
    SHOW_TIME: true
  env:
    GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
